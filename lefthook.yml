# Refer for explanation to following link:
# https://lefthook.dev/configuration/

# Validate commit messages
commit-msg:
  commands:
    "lint commit message":
      run: npx commitlint --edit {1}

# Lint default lean module (may consume long time)
# pre-commit:
#   parallel: true
#   commands:
#     "lint lean module":
#       run: lake exe runLinter
